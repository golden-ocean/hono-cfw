export const GlobalConstants = {
  RootID: "rootid000000000000000000",
};

export enum StatusEnum {
  Enable = "Enable",
  Disable = "Disable",
}

export enum GenderEnum {
  Unknown = "Unknown",
  Male = "Male",
  Female = "Female",
}

export enum DataScopeEnum {
  All = "All",
  Self = "Self",
  Department = "Department",
  Organization = "Organization",
}
